# Repository Instructions

- Do not deploy changes to remote machines with `scp`, ad hoc file copies, or manual sync commands.
- For remotes such as `hermes`, commit/push from this repo and pull the repo on the remote.
- If a remote needs current changes before they are committed, stop and ask how to proceed instead of copying files directly.
